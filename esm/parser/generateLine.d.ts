import { Figure } from "../figures/Figure";
import { Parser } from "../Parser";
export declare function generateLine(parser: Parser, name: string, code: string[], options: string[]): Figure[];
export declare function generatePerpendicular(parser: Parser, name: string, code: string[], options: string[]): Figure[];
export declare function generateParallel(parser: Parser, name: string, code: string[], options: string[]): Figure[];
export declare function generateBissector(parser: Parser, name: string, code: string[], options: string[]): Figure[];
export declare function generateTangent(parser: Parser, name: string, code: string[], options: string[]): Figure[];
export declare function generateVector(parser: Parser, name: string, code: string[], options: string[]): Figure[];
